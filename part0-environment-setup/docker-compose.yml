services:
  workshop-setup:
    build: .
    container_name: mcp-workshop-setup
    stdin_open: true
    tty: true
    volumes:
      - ./:/home/workshop/setup-instructions/
    ports:
      - "4000:3000"   # create-react-app
      - "6173:5173"   # Vite
      - "10000:8000"  # Django
